{
  "name": "medicine-book",
  "version": "1.0.0",
  "description": "Medicine tracking application",
  "scripts": {
    "build:frontend": "npm --prefix ./front-end run build",
    "deploy:functions": "firebase deploy --only functions",
    "deploy:hosting": "firebase deploy --only hosting",
    "deploy:all": "npm run build:frontend && firebase deploy",
    "serve:frontend": "npm --prefix ./front-end run dev"
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^6.6.3",
    "@testing-library/react": "^16.3.0",
    "firebase-tools": "^14.3.1",
    "jsdom": "^26.1.0",
    "vitest": "^3.1.3"
  },
  "engines": {
    "node": "20"
  },
  "private": true
}
